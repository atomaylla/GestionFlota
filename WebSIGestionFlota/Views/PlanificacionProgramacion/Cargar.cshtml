@{
    ViewBag.Title = "View1";

}

@section scripts {
    <script src="@Url.Content("~/Scripts/moment.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/fullcalendar.js")" type="text/javascript"></script>
    <script src="~/Scripts/page.js"></script>

}


   
  
    
    <p></p>
    <div class="form-group">
        <label for="exampleInputEmail1">Conductor </label>
        <select name="conductor" id="conductor" class="form-control">
            <option value="0">Seleccionar</option>
            @foreach (var item in ViewData["Conductores"] as List<EntityLayer.Conductor>)
            {

                <option value="@item.codConductor">
                    @item.nomConductor
                </option>
            }
        </select>
    </div>
    @*<div class="form-group">
        <label for="exampleInputEmail1">Conductor 2</label>
        <select name="origen" class="form-control">
            <option value="0">Seleccionar</option>
            @foreach (var item in ViewData["Conductores"] as List<EntityLayer.Conductor>)
            {

                <option value="@item.codConductor">
                    @item.nomConductor
                </option>
            }
        </select>
    </div>*@
    <div class="form-group">
        <label for="exampleInputEmail1">Unidad</label>
        <select name="unidad" id="unidad" class="form-control">
            <option value="0">Seleccionar</option>
            @foreach (var item in ViewData["Unidades"] as List<EntityLayer.TractorCisterna>)
            {

                <option value="@item.codConfiguracion">
                    @item.nroPlacaCisterna-@item.nroPlacaTractor
                </option>
            }
        </select>
    </div>
      



  
