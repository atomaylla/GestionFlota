
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@*@foreach (var item in (List<string>)@ViewData["usuarios"])*@

@foreach (var item in Model)
{
    @item.codUsuario @item.desUsuario @item.pasUsuario
}